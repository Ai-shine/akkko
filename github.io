---
layout: post
title: "Из чего состоит доменное имя моего сайта"
date: 2026-01-13 10:00:00 +0300
categories: blog dns
---

<a href="mailto:ваша-почта@example.com">Напишите мне</a>

Доменное имя — это удобочитаемый адрес сайта в интернете, который заменяет сложный числовой IP-адрес. Например, вместо `185.199.108.153` мы пишем `example.com`.

Моё доменное имя (например, `username.github.io`) состоит из нескольких уровней иерархии, расположенных **справа налево** — от самого общего к самому конкретному.

Вот как выглядит разбор на примере типичного сайта на GitHub Pages:

### 🔹 Уровень 1: Корневая зона (Root Zone)
Обозначается точкой `.`, но обычно не отображается. Это вершина всей DNS-иерархии.

### 🔹 Уровень 2: Домен верхнего уровня (Top-Level Domain, TLD)
Пример: `.io`  
Это категория домена — может быть географической (`.ru`, `.de`) или тематической (`.com`, `.org`, `.io`, `.blog` и т.д.).

### 🔹 Уровень 3: Второй уровень (Second-Level Domain, SLD)
Пример: `github` в `github.io`  
Это основное имя, которое регистрирует владелец. В случае GitHub Pages — это имя платформы.

### 🔹 Уровень 4: Поддомен третьего уровня (Third-Level Domain / Subdomain)
Пример: `username` в `username.github.io`  
Это часть, которую создаёт **вы** — пользователь GitHub. Она указывает на ваш конкретный репозиторий и сайт.

---

Таким образом, полное доменное имя `username.github.io` состоит из:

| Уровень | Имя         | Назначение                     |
|--------|--------------|-------------------------------|
| 4      | `username`   | Ваш личный поддомен            |
| 3      | `github`     | Основной сервис (GitHub)       |
| 2      | `.io`        | Домен верхнего уровня (TLD)    |
| 1      | `.` (скрыт)  | Корневая зона DNS              |

Каждый уровень делегирован вышестоящему: корень управляет TLD, TLD управляет `github.io`, а GitHub позволяет вам создавать поддомены вида `ваш-логин.github.io`.

---

Если у вас есть собственный кастомный домен (например, `mysite.com`), структура будет проще:
- `.com` — TLD
- `mysite` — второй уровень (ваш зарегистрированный домен)

А поддомены (например, `blog.mysite.com`) добавляются вами по желанию.

---

💡 **Интересный факт**: DNS работает «справа налево», потому что система делегирования начинается с корня и спускается вниз по иерархии.

---

Спасибо за внимание!  
— Автор
